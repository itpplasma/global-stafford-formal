# Global Stafford v1.0.2

Fixes Palomar's metadata rejection by supplying `related_formalizations[0].id`: the version DOI of the imported Stafford38 v1.1.0 formalization, https://doi.org/10.5281/zenodo.22667041.

The original file reproduced the missing-identifier error. The corrected file passes `load_formalization_metadata` from PalomarRegistry/PalomarSubmission at `ef2fa1eadcb246c2346ddba39b52eaa53d4bb763`. This validates metadata; it does not claim proof acceptance. The reported failed submission stopped at metadata validation.

Proof sources, Challenge/Solution, Comparator configuration, dependency pins and overview PDF are unchanged from v1.0.1. The previously recorded new-pin proof replay remains pending.

For Palomar, use the full commit at tag v1.0.2 with `comparator.json`, `formalization.yaml` and the repository root. A rerun of the old submission commit will retain the old error.
